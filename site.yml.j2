---
# site.yml for {{ role_name }}

- name: Project {{ role_name }}
  hosts: all
  gather_facts: true
  vars:                           # delete me if not needed
    some_variable: some_value     # delete me if not needed

  tasks:
    - name: some role
      ansible.builtin.include_role:
        name: some role
      vars:    # delete me if not needed
        # add variables if needed else delete entire section

...
